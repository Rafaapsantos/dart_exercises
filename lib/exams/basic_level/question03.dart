/// Seja a função [mathPow] abaixo que recebe dois parâmetros [a] e [n] que são do tipo int.
///
/// Além disso essa função retorna um inteiro que representa o resultado da base b elevada a um numero n: bˆn.
///
/// Neste exercício você deve projetar a função de modo que de acordo com a variável [a] e [n] recebidas como parâmetro
/// ela retorne a base b elevada ao numero n: bˆn
///
/// Por exemplo, se a = 2 e n = 3, então devemos retornar 8.
///

int mathPow(int b, int n) {
  throw UnimplementedError('Você deve implementar essa função');
}
